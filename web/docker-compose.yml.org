version: '3'

services:
  php:
    restart: always
    build:
      context: .
      dockerfile: ./php/Dockerfile
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
#      - type: bind
#        source: "./tmp"
#        target: "/"
  nginx:
    restart: always
    ports:
      - 8008:80
    build:
      context: .
      dockerfile: ./nginx/Dockerfile

